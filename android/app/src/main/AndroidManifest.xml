<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Background location trail: coarse+fine while in use, BACKGROUND to keep going
         when the app is closed, a foreground service to stay alive, notifications for
         its persistent status, and a boot receiver to resume after a reboot. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Smart polling: Activity Recognition lets us sleep the GPS while still and wake
         on motion (saves battery, like OwnTracks/Life360). Optional — without it we
         fall back to continuous fixes. (Pre-29 used the GMS-namespaced permission.) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JBrainTracker">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.JBrainTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Background location trail: a foreground service (type=location) that batches
             fixes to /api/locations/bulk, and a receiver to resume it after boot. -->
        <service
            android:name=".LocationService"
            android:exported="false"
            android:foregroundServiceType="location" />
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Receives activity-transition callbacks (still ⇄ moving) to gate the GPS. -->
        <receiver
            android:name=".ActivityReceiver"
            android:exported="false" />

        <!-- Receives notes dictated on the paired watch (relayed over the Wear Data
             Layer) and forwards them to JBrain with this phone's configured key. -->
        <service
            android:name=".NoteRelayService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/jbrain/note" />
            </intent-filter>
        </service>

    </application>
</manifest>
