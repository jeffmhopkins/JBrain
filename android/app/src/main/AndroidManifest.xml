<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Background location trail: coarse+fine while in use, BACKGROUND to keep going
         when the app is closed, a foreground service to stay alive, notifications for
         its persistent status, and a boot receiver to resume after a reboot. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Smart polling: Activity Recognition lets us sleep the GPS while still and wake
         on motion (saves battery, like OwnTracks/Life360). Optional — without it we
         fall back to continuous fixes. (Pre-29 used the GMS-namespaced permission.) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />

    <!-- The capture widgets shoot via the system camera and dictate via the system
         speech recognizer, so this app needs neither CAMERA nor RECORD_AUDIO — those
         apps own the grants. Declared optional so a camera-less device can still install
         (and still use the dictation widget). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JBrainTracker">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.JBrainTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Background location trail: a foreground service (type=location) that batches
             fixes to /api/locations/bulk, and a receiver to resume it after boot. -->
        <service
            android:name=".LocationService"
            android:exported="false"
            android:foregroundServiceType="location" />
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Receives activity-transition callbacks (still ⇄ moving) to gate the GPS. -->
        <receiver
            android:name=".ActivityReceiver"
            android:exported="false" />

        <!-- Receives notes dictated on the paired watch (relayed over the Wear Data
             Layer) and forwards them to JBrain with this phone's configured key. -->
        <service
            android:name=".NoteRelayService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/jbrain/note" />
            </intent-filter>
        </service>

        <!-- Invisible one-tap bridge behind both home-screen widgets: fires the system
             camera / speech recognizer, hands the result to UploadWorker, and finishes.
             Translucent so nothing flashes; not exported (only our widgets launch it). -->
        <activity
            android:name=".CaptureActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Hands the camera app a content:// URI to write the captured JPEG to. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- One-tap photo widget: tap → camera → photo uploaded (auto-described). -->
        <receiver
            android:name=".PhotoWidgetProvider"
            android:exported="true"
            android:label="@string/widget_photo_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_photo_info" />
        </receiver>

        <!-- One-tap dictation widget: tap → speak → transcript saved as a note. -->
        <receiver
            android:name=".DictateWidgetProvider"
            android:exported="true"
            android:label="@string/widget_dictate_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_dictate_info" />
        </receiver>

    </application>
</manifest>
