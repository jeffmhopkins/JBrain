fastapi==0.115.6
uvicorn[standard]==0.34.0
anthropic==0.42.0
openai==1.59.6          # xAI Grok via its OpenAI-compatible API (base_url=api.x.ai/v1)
pydantic-settings==2.7.1
python-multipart==0.0.20
sqlite-vec==0.1.6
fastembed==0.5.1
PyYAML==6.0.2
Jinja2==3.1.6          # action-engine templating (pipeline.py): SandboxedEnvironment
croniter==3.0.3
tzdata==2024.2
pypdf==5.1.0
pdfplumber==0.11.4   # geometry-aware PDF parsing (lab-result tables: map values->date columns by x)
Pillow==10.4.0   # fastembed 0.5.1 requires pillow <11.0.0,>=10.3.0
pywebpush==2.0.0   # Web Push (VAPID signing + RFC8291 payload encryption); pulls py-vapid, http-ece
# Test-only deps (pytest, httpx) live in requirements-dev.txt — not in the image.
# OPTIONAL image/screenshot/scanned-PDF lab intake deps (pytesseract, pypdfium2 + the system
# tesseract-ocr binary) live in requirements-vision.txt — the text-PDF path needs none of them.
